{
  "providers": {
    "php": {
      "version": "8.2",
      "extensions": ["pdo", "pdo_mysql", "mbstring", "tokenizer", "xml", "ctype", "fileinfo", "bcmath", "curl", "openssl"]
    }
  },
  "deploy": {
    "startCommand": "php artisan storage:link --force && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}